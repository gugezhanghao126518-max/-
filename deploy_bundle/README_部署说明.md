# 文章采集助手（服务器网页版）部署包

> 给不会命令行的同学：把本目录发给运维/同事，照着 `一键部署命令.txt` 执行即可。

## 包含内容
- `服务器部署文档_完整版.md`：从0到1部署、开机自启、Nginx 反代、常见故障。
- `一键部署命令.txt`：最短命令路径。
- `systemd_service_example.service`：开机自启模板。
- `nginx_reverse_proxy_example.conf`：域名反代模板。
- `运维交付清单.md`：部署完成后要回传给你的信息。

## 默认端口
- 7860
